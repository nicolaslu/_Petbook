<div class="well" id="block_menu">
	<div class="span3" id="block_avatar">
		{% if app.user.id == app.session.get('pageUserId') %}
			<a href="{{ path('all_photos')}}"><img src="{{asset('img/uploads/%s') | format( app.session.get('path'))}}" alt="avatar" id="avatar"/></a>
		{% else %}
			<img src="{{asset('img/uploads/%s') | format( app.session.get('path'))}}" alt="avatar" id="avatar"/>
		{% endif %}
		{{app.session.get('username')}}
	</div>

	<br/><br/><br/>
	<hr class="barre">
	<h4>Menu</h4>
	<ul class="nav nav-pills nav-stacked" style="max-width: 300px;">
	  <li><a href="{{ path('social_contact_list') }}">Contact list</a></li>
	  <li><a href="{{ path('wall_logged') }}">Wall</a></li>
	  <li><a href="{{ path('all_photos') }}">My photos</a></li>
	  <li><a href="/animals">My animals</a></li>
	</ul>

</div>